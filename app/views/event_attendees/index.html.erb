<h2>Event.name: <%= @event.name%>  </h2>

<h2>Attendee-list  </h2>

<table class="table">	
<% @attendees.each do |a|%>
	<tr>
		<td>
		<%= a.name %>
		</td>

		<td>
		<%= link_to "Show", event_attendee_path(@event, a), :class => "btn btn-primary"%>

		

		<%= link_to "Edit", edit_event_attendee_path(@event, a), :class => "btn btn-default"%>

		<%= link_to "Delete", event_attendee_path(@event, a), :method =>"delete", :data => { :confirm => "Sure?"}, :class => "btn btn-danger"%>

		</td>
	</tr>


<% end %>
</table>


<%= link_to "New Attendee", new_event_attendee_path(@event), :class => "btn btn-primary" %>

<hr>
<%= link_to "Back to Events index", events_path %>